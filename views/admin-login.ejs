<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - OnGrounds</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        :root {
            --primary-color: #331B3F;
            --secondary-color: #ACC7B4;
            --hover-color: #4a2959;
            --error-color: #e74c3c;
            --text-light: #ffffff;
            --text-dark: #2c3e50;
            --bg-light: #f5f5f5;
        }

        body {
            background-color: var(--bg-light);
            font-family: 'Arial', sans-serif;
            position: relative;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            opacity: 0.1;
            z-index: -1;
        }

        .admin-login-container {
            max-width: 400px;
            margin: 100px auto;
            padding: 2.5rem;
            background-color: #fff;
            border-radius: 12px;
            box-shadow: 0 8px 24px rgba(51, 27, 63, 0.15);
            border: 1px solid rgba(172, 199, 180, 0.3);
        }

        .admin-login-header {
            text-align: center;
            margin-bottom: 2.5rem;
        }

        .admin-login-header h1 {
            color: var(--primary-color);
            font-size: 2.2rem;
            margin-bottom: 0.75rem;
            font-weight: 600;
        }

        .admin-login-header p {
            color: var(--text-dark);
            margin-bottom: 1rem;
            opacity: 0.8;
        }

        .admin-login-form {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .form-group label {
            font-weight: 500;
            color: var(--primary-color);
            font-size: 0.95rem;
        }

        .form-group input {
            padding: 1rem;
            border: 2px solid var(--secondary-color);
            border-radius: 8px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background-color: rgba(172, 199, 180, 0.1);
        }

        .form-group input:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(51, 27, 63, 0.1);
            background-color: #fff;
        }

        .login-btn {
            background-color: var(--primary-color);
            color: var(--text-light);
            padding: 1rem;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 1rem;
            position: relative;
            overflow: hidden;
        }

        .login-btn:hover {
            background-color: var(--hover-color);
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(51, 27, 63, 0.2);
        }

        .login-btn:active {
            transform: translateY(0);
            box-shadow: 0 2px 6px rgba(51, 27, 63, 0.1);
        }

        .login-btn:disabled {
            background-color: var(--secondary-color);
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }

        .error-message {
            color: var(--error-color);
            font-size: 0.875rem;
            margin-top: 0.5rem;
            padding: 0.75rem;
            background-color: rgba(231, 76, 60, 0.1);
            border-radius: 6px;
            display: none;
            border: 1px solid rgba(231, 76, 60, 0.2);
        }

        .loading {
            display: none;
            text-align: center;
            margin-top: 1rem;
        }

        .loading-spinner {
            border: 3px solid rgba(51, 27, 63, 0.1);
            border-top: 3px solid var(--primary-color);
            border-radius: 50%;
            width: 24px;
            height: 24px;
            animation: spin 1s linear infinite;
            margin: 0 auto;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        .form-group input {
            transform: translateY(0);
            transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .form-group input:focus {
            transform: translateY(-2px);
        }
    </style>
</head>
<body>
    <div class="admin-login-container">
        <div class="admin-login-header">
            <h1>Admin Login</h1>
            <p>Please enter your credentials to continue</p>
        </div>
        <form id="adminLoginForm" class="admin-login-form">
            <div class="form-group">
                <label for="username">Username</label>
                <input type="text" id="username" name="username" required>
            </div>
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required>
            </div>
            <div id="errorMessage" class="error-message"></div>
            <div id="loading" class="loading">
                <div class="loading-spinner"></div>
            </div>
            <button type="submit" class="login-btn" id="loginButton">Login</button>
        </form>
    </div>

    <script src="/js/admin-login.js"></script>
</body>
</html> 